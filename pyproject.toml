[project]
name = "kaigara"
version = "0.1.0"
description = "Secure agent stack with container-isolated secrets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "httpx>=0.28",
]
